❌ verify failed (retryable) — #{{ issue_number }} — attempt {{ attempt_count }} / {{ max_retry_count }}
Failed check: `{{ failed_command }}`
Action: `mino-run` will retry automatically (Loop Mode), or run `/mino-run issue-{{ issue_number }}` manually.
{{ commit_link_or_empty }}
