# Regression Report: Silent Events & Consolidated Summary — v0.3.0

Fixture repo: `https://github.com/robinv8/mino-skills-fixture-v030`
Date: 2026-04-21
Protocol: v1.10

## TC-10.1: Happy path produces exactly one GitHub comment

**Issue:** #1 (`Add hello.js`)

**Commands executed:**
1. `/task adopt issue-1` → approved
2. `/run issue-1` → created `hello.js`, committed `[run] issue-1: add hello.js`
3. `/verify issue-1` → `node -e "require('fs').existsSync('hello.js')"` passed
4. `/checkup done issue-1` → rendered terminal summary, posted comment

**Evidence:**
- Issue #1 comment count: **1**
- Comment body starts with: `🏁 Issue 1 done — add-hello-js`
- Inlined yml blocks in comment: 5 (`task_adopted`, `run_started`, `run_completed`, `verify_passed`, `checkup_done`)
- Local events: `.mino/events/issue-1/*.yml` = 5 files
- Labels: `iron-tree:adopted`, `stage:done`

**Result:** PASS

---

## TC-10.2: Verify failure produces immediate audible comment

**Issue:** #2 (`Add failing test`)

**Commands executed:**
1. `/task adopt issue-2` → approved
2. `/run issue-2` → created `test.js` with `throw new Error('intentional failure')`, committed
3. `/verify issue-2` → `node test.js` failed with intentional failure

**Evidence:**
- Issue #2 comment count: **1**
- Comment body starts with: `❌ verify failed (retryable) — issue-2`
- Contains yml block with `event: verify_failed_retryable`
- Contains literal line: `Local events: \`.mino/events/issue-2/\``
- Local events: 4 files (`task_adopted`, `run_started`, `run_completed`, `verify_failed_retryable`)
- Labels: `stage:run` (not moved to `stage:done`)

**Result:** PASS

---

## TC-10.3: Reconcile from local events (primary source)

**Issue:** #1 (TC-10.1 completed state)

**Command:** `/checkup reconcile issue-1`

**Evidence:**
- Local events found: 5 files matching `approved_revision: 07077d47`
- Report output: `local events: 5 file(s), primary source used`
- No new comment posted (reconcile only emits on drift)

**Result:** PASS

---

## TC-10.4: Reconcile fallback from terminal summary

**Issue:** #1 (TC-10.1 completed state)

**Setup:** Deleted `.mino/events/issue-1/` and `.mino/briefs/issue-1.md`

**Command:** `/checkup reconcile issue-1`

**Evidence:**
- Primary source empty (0 files after deletion)
- Terminal summary comment matched (`🏁 Issue 1 done`)
- Parsed 5 inlined yml blocks from summary
- Rebuilt `.mino/events/issue-1/*.yml` (5 files)
- Report output: `local events: 0 file(s), terminal summary used, rebuilt 5 event(s)`

**Result:** PASS

---

## TC-10.5: Reconcile fallback for legacy v1.9 issue

**Issue:** #4 (`Legacy issue`)

**Setup:** Issue created with 4 separate per-event comments (no terminal summary). Local events absent.

**Command:** `/checkup reconcile issue-4`

**Evidence:**
- Primary source empty
- Terminal summary signature not matched
- Parsed 4 yml event blocks from legacy per-event comments
- Rebuilt `.mino/events/issue-4/*.yml` (4 files)
- Report output: `local events: 0 file(s), legacy per-event comments used, rebuilt 4 event(s)`

**Result:** PASS

---

## TC-10.6: Halt comment carries local-events pointer

**Issue:** #3 (`Add goodbye.js`)

**Setup:** Configured `.git/hooks/pre-commit` to exit 1 (reject all commits)

**Command:** `/run issue-3`

**Evidence:**
- Issue #3 comment count: **1**
- Comment body contains literal line: `Local events: \`.mino/events/issue-3/\`` directly above the yml block
- Comment yml block has `event: run_commit_failed`

**Result:** PASS

---

## Summary

| TC | Description | Result |
|---|---|---|
| TC-10.1 | Happy path → exactly 1 terminal summary comment | PASS |
| TC-10.2 | Verify failure → audible comment with pointer | PASS |
| TC-10.3 | Reconcile from local events (primary) | PASS |
| TC-10.4 | Reconcile fallback from terminal summary | PASS |
| TC-10.5 | Reconcile fallback from legacy v1.9 comments | PASS |
| TC-10.6 | Halt comment carries local-events pointer | PASS |

**Total: 6 / 6 PASS**
